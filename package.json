{
  "name": "cs-portfolio",
  "version": "1.0.0",
  "description": "Full-stack portfolio project with Next.js frontend and Node.js backend",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server:dev\" \"npm run client:dev\"",
    "build": "npm run client:build && npm run server:build",
    "start": "npm run server:start",
    "server:dev": "cd cs_portfolio_server && npm run dev",
    "server:start": "cd cs_portfolio_server && npm start",
    "server:build": "cd cs_portfolio_server && npm run build",
    "client:dev": "cd cs_portfolio_client && npm run dev",
    "client:build": "cd cs_portfolio_client && npm run build",
    "install:all": "npm install && cd cs_portfolio_server && npm install && cd ../cs_portfolio_client && npm install",
    "clean": "cd cs_portfolio_server && rm -rf node_modules dist && cd ../cs_portfolio_client && rm -rf node_modules .next"
  },
  "keywords": [
    "portfolio",
    "nextjs",
    "nodejs",
    "typescript",
    "mysql"
  ],
  "author": "Christian Sheen",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}